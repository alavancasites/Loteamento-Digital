<?php

/**
 * This is the model base class for the table "interesse".
 * DO NOT MODIFY THIS FILE! It is automatically generated by giix.
 * If any changes are necessary, you must set or override the required
 * property or method in class "Interesse".
 *
 * Columns in table "interesse" available as properties of the model,
 * and there are no model relations.
 *
 * @property integer $idinteresse
 * @property string $ip
 * @property string $data
 * @property string $nome
 * @property string $email
 * @property string $telefone
 * @property string $empresa
 * @property string $dia
 * @property string $hora
 * @property string $mensagem
 * @property string $aceito_whatsapp
 * @property string $aceito_email
 * @property string $ativo
 *
 */
abstract class BaseInteresse extends GxActiveRecord {
	
    
        
	public static function model($className=__CLASS__) {
		return parent::model($className);
	}

	public function tableName() {
		return 'interesse';
	}

	public static function label($n = 1) {
		return Yii::t('app', 'Interesse|Interesses', $n);
	}

	public static function representingColumn() {
		return array('ip');
	}

	public function rules() {
		return array(
			array('ip, nome, email, telefone, empresa, hora', 'length', 'max'=>100),
			array('aceito_whatsapp, aceito_email, ativo', 'length', 'max'=>1),
			array('data, dia, mensagem', 'safe'),
      array('email', 'email'),
      array('nome, email, telefone, empresa', 'required'),
			array('ip, data, nome, email, telefone, empresa, dia, hora, mensagem, aceito_whatsapp, aceito_email, ativo', 'default', 'setOnEmpty' => true, 'value' => null),
			array('idinteresse, ip, data, nome, email, telefone, empresa, dia, hora, mensagem, aceito_whatsapp, aceito_email, ativo', 'safe', 'on'=>'search'),
		);
	}

	public function relations() {
		return array(
		);
	}

	public function pivotModels() {
		return array(
		);
	}

	public function attributeLabels() {
		return array(
			'idinteresse' => Yii::t('app', 'Idinteresse'),
			'ip' => Yii::t('app', 'Ip'),
			'data' => Yii::t('app', 'Data'),
			'nome' => Yii::t('app', 'Nome'),
			'email' => Yii::t('app', 'Email'),
			'telefone' => Yii::t('app', 'Telefone'),
			'empresa' => Yii::t('app', 'Empresa'),
			'dia' => Yii::t('app', 'Dia'),
			'hora' => Yii::t('app', 'Hora'),
			'mensagem' => Yii::t('app', 'Mensagem'),
			'aceito_whatsapp' => Yii::t('app', 'Aceito Whatsapp'),
			'aceito_email' => Yii::t('app', 'Aceito Email'),
			'ativo' => Yii::t('app', 'Ativo'),
		);
	}

	public function search() {
		$criteria = new CDbCriteria;

		$criteria->compare('idinteresse', $this->idinteresse);
		$criteria->compare('ip', $this->ip, true);
		$criteria->compare('data', $this->data, true);
		$criteria->compare('nome', $this->nome, true);
		$criteria->compare('email', $this->email, true);
		$criteria->compare('telefone', $this->telefone, true);
		$criteria->compare('empresa', $this->empresa, true);
		$criteria->compare('dia', $this->dia, true);
		$criteria->compare('hora', $this->hora, true);
		$criteria->compare('mensagem', $this->mensagem, true);
		$criteria->compare('aceito_whatsapp', $this->aceito_whatsapp, true);
		$criteria->compare('aceito_email', $this->aceito_email, true);
		$criteria->compare('ativo', $this->ativo, true);

		return new CActiveDataProvider($this, array(
			'criteria' => $criteria,
		));
	}
  public function afterSave(){
    $message = new YiiMailMessage;
    $message->view = 'interesse';
    $message->setBody(array('interesse' => $this),'text/html','latin1');
    $message->subject = 'Interesse'.' - '.date('d/m/Y H:i:s');
    $message->addTo(Yii::app()->params['email_contato']);
    $message->setReplyTo($this->email);
    $message->setFrom(array('noreply@popupdigital.com.br'=>Yii::app()->name));
    Yii::app()->mail->send($message);
  }

}